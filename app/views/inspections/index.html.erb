<h1>Inspections</h1>

<table>
  <tr>
    <th>Name</th>
    <th># Observables</th>
    <!--<th>Notes</th>-->
    <!--<th>Started at</th>-->
    <th>Finished at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @inspections.each do |inspection| %>
  <tr>
    <td><%= inspection.name %></td>
    <td><%= inspection.total_observables %></td>
    <!--<td><%= truncate(inspection.notes, :length => 30) %></td>-->
    <!--<td><%= inspection.started_at %></td>-->
    <td><%= inspection.finished_at %></td>
    <td><%= link_to 'Show', inspection %></td>
    <td><%= link_to 'Edit', edit_inspection_path(inspection) %></td>
    <td><%= link_to 'Destroy', inspection, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Inspection', new_inspection_path %>
